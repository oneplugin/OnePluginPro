{% extends "_layouts/cp" %}

{% set fullPageForm = true %}

{% set crumbs = [
    { label: settings.pluginName, url: url("one-plugin-pro") },
    { label: "Settings"|t("one-plugin-pro"), url: url("one-plugin-pro/settings") },
]
%}

{% block actionButton %}
    <div class="buttons">
        <div class="btngroup submit">
            <input type="submit" class="btn submit" value="{{ 'Save'|t('one-plugin-pro') }}">
        </div>
    </div>
{% endblock %}
